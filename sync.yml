trigger:
  branches:
    include:
    - main

resources:
  repositories:
    - repository: templates
      type: git
      name: Plumbing/core-templates
    - repository: extension-lists
      type: git
      name: Open/extension-lists

stages:
- template: stages/sync/sync.yml@templates  # Template reference
  parameters:
    checkout: extension-lists
